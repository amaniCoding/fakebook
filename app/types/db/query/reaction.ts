export type FirstReactorInfo = {
  reactionId: string;
  reactionType: string;
  reactor: string;
};

export type ReactionInfo = {
  reactionid: string;
  reactiontype: string;
  fname: string;
  lname: string;
};
